# Cursor Chat Manager
#
# 网页/CLI 模式：无需任何第三方依赖，仅用 Python 标准库。
#     python3 app.py
#
# 桌面 App 模式（原生窗口，不开浏览器）：需要 pywebview。
#     pip3 install pywebview
#     python3 desktop.py
#
# 打包为可双击的 .app（可选）：需要 pyinstaller。
#     pip3 install pyinstaller
#     ./build_app.sh
#
# 要求: Python >= 3.8 (已在 3.12 上测试)

pywebview>=5.0   ; 仅桌面 App 模式需要
